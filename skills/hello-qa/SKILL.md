---
name: hello-qa
description: 实现完成后做质量自检，或用户输入 ~qa 或 ~hello-qa 时使用：运行真实验证命令，系统性检查后如实报告。
---

# 质量自检

## 此刻该怎么想

质量结论来自运行结果，不来自自我评价。检查的目的是发现问题，不是产出一份“检查过了”的证明。本技能的对象是你刚完成的工作；要对任意对象（仓库、插件、技能、文档等）做独立的全量审查，使用 hello-eva 技能（~eva）。

1. 找到项目的真实验证命令，按优先级：方案 frontmatter 的 verify 字段 → 项目脚本（package.json scripts、Makefile 等）→ 项目文档。逐条运行。
2. 失败就修复，修复后重新运行，直到通过；无法修复的如实说明原因与影响。
3. 把关键的原始输出贴给用户（通过多少、失败什么），不要转述成“测试都通过了”。

每项关键检查的结论只有四种：通过（怎么验证的）、失败（怎么复现）、无法验证（缺什么条件）、不适用（为什么）。“无法验证”不是“通过”，也不能写成“存在问题”。

## 系统性检查

在验证命令之外，对照四个角度看一遍改动：

- 需求覆盖：逐条对照目标或验收标准，确认没有漏项，也没有做需求之外的事。
- 边界与失败路径：空输入、异常输入、并发、权限不足时的行为是否合理；错误是否被吞掉。
- 过度工程：多余的抽象、无消费者的文件、自证性文档，与功能缺陷同级，发现即指出并清理。
- 敏感信息：代码与输出中没有硬编码的密钥、凭据、隐私数据。

## 报告问题的纪律

- 每个问题指向具体位置（文件、行号或复现步骤），说清期望与实际的差异。
- 把握不足就标“疑似”，并写明缺少哪部分证据；宁可标疑似，不把不确定写成确定，也不因为不确定而隐去。
- 同一根因的多处表现合并为一条，列出全部位置；不为数量凑条目。
- 写下之前先试着驳倒自己：真的可触发吗？有没有让它其实成立的上下文？驳不倒再写。

## 如实报告

- 跑过什么、结果如何、没跑什么、为什么没跑，全部明说。
- “没发现问题”也要给出可信度来源：说明实际检查与验证了什么，覆盖到哪里为止。
- 项目没有任何验证命令时，说明这一点，并给出一条最小的验证建议，由用户决定是否采纳。

## 交付前自问

- 结论里的每一句，是有运行结果支撑，还是我自己觉得？
- 有没有把“无法验证”当成了“通过”，或把“缺少证据”写成了“存在错误”？
- 有没有为了“完成检查”而检查的项目？有就删。
